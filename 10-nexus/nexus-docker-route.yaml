apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nexus-registry
  namespace: nexus
spec:
  host: $(NEXUS_REGISTRY_HOST)
  to:
    kind: Service
    name: nexus-docker
  port:
    targetPort: docker
  tls:
    termination: edge
